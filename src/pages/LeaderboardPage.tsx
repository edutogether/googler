import { Flag } from 'lucide-react';
import type { LeaderboardEntry } from '../data/appServices';

const barColors = [{ fill: 'bg-red-400', bg: 'bg-red-50', border: 'border-red-200' }, { fill: 'bg-orange-400', bg: 'bg-orange-50', border: 'border-orange-200' }, { fill: 'bg-green-400', bg: 'bg-green-50', border: 'border-green-200' }, { fill: 'bg-blue-400', bg: 'bg-blue-50', border: 'border-blue-200' }, { fill: 'bg-purple-400', bg: 'bg-purple-50', border: 'border-purple-200' }, { fill: 'bg-pink-400', bg: 'bg-pink-50', border: 'border-pink-200' }, { fill: 'bg-teal-400', bg: 'bg-teal-50', border: 'border-teal-200' }, { fill: 'bg-indigo-400', bg: 'bg-indigo-50', border: 'border-indigo-200' }];

export function LeaderboardPage({ rankings, userId }: { rankings: LeaderboardEntry[]; userId?: string }) {
  const sortedRankings = [...rankings].sort((a, b) => { const totalA = (a.scoreL1 || 0) + (a.scoreL2 || 0); const totalB = (b.scoreL1 || 0) + (b.scoreL2 || 0); if (totalA !== totalB) return totalB - totalA; return Number(b.passedL1) + Number(b.passedL2) - Number(a.passedL1) - Number(a.passedL2); });
  return <div className="max-w-3xl mx-auto px-4 py-8 animate-fade-in space-y-6"><div className="bg-gradient-to-r from-[#34A853] to-[#1E8E3E] p-8 rounded-[2rem] shadow-sm text-white text-center relative overflow-hidden"><div className="absolute inset-0 opacity-20 bg-[radial-gradient(circle_at_center,_var(--tw-gradient-stops))] from-white to-transparent blur-xl"></div><h2 className="text-2xl md:text-3xl font-black mb-2 relative z-10 flex justify-center items-center gap-2"><Flag className="w-8 h-8 text-white" /> Google Educator가 되기 위한 여정</h2><p className="text-white/90 font-medium relative z-10 text-sm">L1과 L2를 합쳐 총 60개의 미션! 최종 목적지를 향해 달려요 🏁</p></div><div className="bg-white p-6 rounded-[2rem] shadow-sm border border-[#E8EAED] space-y-4 relative"><div className="absolute right-[12%] top-6 bottom-6 w-1 bg-[#EA4335]/20 border-r-2 border-dashed border-[#EA4335] z-0 hidden sm:block"></div><span className="absolute right-[9%] top-2 text-[10px] font-black text-[#EA4335] hidden sm:block">FINISH</span>{sortedRankings.length === 0 && <p className="text-center text-[#9AA0A6] py-10 font-bold">아직 레이스에 참여한 멤버가 없습니다! 🏃💨</p>}{sortedRankings.map((rank, idx) => { const totalScore = (rank.scoreL1 || 0) + (rank.scoreL2 || 0); const hasBoth = rank.passedL1 && rank.passedL2; const percent = hasBoth ? 100 : Math.min((totalScore / 60) * 85, 85); const isMe = rank.uid === userId; const colorTheme = barColors[idx % barColors.length]; return <div key={rank.uid} className={`relative w-full h-16 ${colorTheme.bg} rounded-2xl border ${colorTheme.border} overflow-hidden flex items-center shadow-sm ${isMe ? 'ring-2 ring-offset-1 ring-blue-500' : ''}`}><div className={`absolute left-0 top-0 bottom-0 ${colorTheme.fill} transition-all duration-1000 z-0 opacity-40`} style={{ width: `${percent}%` }}></div><div className="absolute left-4 font-black text-sm text-[#202124] z-10 flex items-center gap-2"><span className={`w-7 h-7 flex items-center justify-center rounded-full text-xs text-white shadow-sm ${idx === 0 ? 'bg-[#FBBC05]' : idx === 1 ? 'bg-slate-400' : idx === 2 ? 'bg-[#CD7F32]' : 'bg-[#BDC1C6]'}`}>{idx + 1}</span><span className="bg-white/90 px-2.5 py-1 rounded-lg shadow-sm whitespace-nowrap border border-[#E8EAED] flex items-center gap-1">{rank.nickname}{isMe && <span className="text-[#1A73E8] text-xs">(나)</span>}</span></div><div className="absolute z-20 transition-all duration-1000 ease-out flex items-center justify-center" style={{ left: `calc(${percent}% - ${hasBoth ? '45px' : '20px'})` }}><div className="relative flex items-center justify-center"><span className="text-3xl drop-shadow-md relative z-10">{rank.emoji || '🏃'}</span>{hasBoth && <span className="absolute -top-4 -right-1 text-2xl drop-shadow-md animate-bounce z-20" style={{ transform: 'rotate(15deg)' }}>👑</span>}{!hasBoth && rank.passedL1 && <span className="absolute -bottom-1 -right-2 text-xl drop-shadow-md z-20">🥇</span>}{!hasBoth && !rank.passedL1 && rank.passedL2 && <span className="absolute -bottom-1 -right-2 text-xl drop-shadow-md z-20">🥈</span>}</div></div><div className="absolute right-4 font-black text-sm z-10 bg-white/90 px-2 py-1 rounded-lg border border-[#E8EAED] text-xs shadow-sm">{totalScore} / 60</div></div>; })}</div></div>;
}
